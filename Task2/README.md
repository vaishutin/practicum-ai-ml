# Задание 2. Подготовка базы знаний
> Теперь вам нужно создать собственную базу знаний, с которой LLM не сталкивалась при обучении. Так вы сможете гарантировать честную работу механизма RAG. Это будет ваш локальный корпоративный мир, невидимый внешнему ИИ.

## Что нужно сделать?
>1. Выберите предметную область
>   - Возьмите открытый фандом или тематическую вики (например, starwars.fandom.com).
>   - Выберите 30+ страниц по ключевым сущностям: персонажи, объекты, технологии, события.

- В качестве домена выбрана [русскоязычная фанбаза по Star Wars](https://starwars.fandom.com/ru/wiki/Заглавная_страница). Отобрано 30 статей (целиком или частично) по темам: `characters, events, places, tech`.
- Сырые материалы сохранены в [task2/raw_db/data](raw_db/data).
>2. Скачайте и очистите тексты
>   - С помощью Python-скрипта или вручную получите из этих страниц чистый текст (без HTML и лишнего форматирования).
>   - Разбейте каждый документ по темам (один файл — одна сущность).`
- документы приведены к Markdown, убраны спецсимволы и лишнее форматирование;
- добавлены заголовки уровней 1–3, чтобы позже использовать их как метаданные;

>3. Замените ключевые термины. Соберите словарь соответствий.
>   Например:
>   "Darth Vader" → "Xarn Velgor"  
>   "Death Star" → "Void Core"  
>   "The Force" → "Synth Flux"   
>   - Используйте скрипт для замены всех упоминаний терминов в текстах.
>   - Убедитесь, что тексты остались логичными и читаемыми, но больше не распознаются как относящиеся к вселенной Star Wars.
- составлен словарь замен [terms_map.json](raw_db/terms_map.json): теннисная тематика, использованы реальные имена теннисистов и термины;
- скрипт [replace_terms.py](raw_db/replace_terms.py) собирает из сырой базы уникальную RAG-базу, переименовывая часть сущностей;
- цель переименований — проверить, что бот отвечает на основе RAG, а не из предобученных знаний;
  - >многие открытые источники (фандомы, Википедия и т. д.) уже входили в датасеты LLM; если модель отвечает «по памяти», задание теряет смысл.
- пример запуска скрипта из корня `task2`:
    ```sh
    python raw_db/replace_terms.py \\  
        --src raw_db/data \\  
        --dest knowledge_base \\  
        --terms raw_db/terms_map.json
    ```

> 4. Сохраните уникальную базу
>    - Папка `knowledge_base/`, в ней — очищенные и переименованные .txt или .md документы (30+ штук).
>    - Дополнительно ещё `terms_map.json` со словарём замен (исходное → вымышленное).
- итоговая RAG-база лежит в [knowledge_base](knowledge_base).
- словарь [terms_map.json](raw_db/terms_map.json) и база [knowledge_base](knowledge_base) пересоздавались несколько раз: ошибки замены находились вручную и по результатам тестов LLM в задании 4.
